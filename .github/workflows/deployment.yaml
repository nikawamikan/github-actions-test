name: Deployment

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/test.yaml/@main
  deployment:
    name: deployment
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          cat /etc/os-release

      